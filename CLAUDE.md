# 教资备考项目指南

> 本文件每次新对话自动加载。改规则、目录、答题风格直接编辑本文件。

## 一、项目说明

> 需要具体文件时，直接 `find` 或 `ls` 扫目录，不要依赖记忆。
> 大文件先 grep 关键词定行号再按行 Read（课标全文36万字，切勿整读）。
> 涉及复杂公式或逐字核对时，回读 `笔试/外部资料/` 下的原始 PDF/图片。
> **课标PDF页码**：HTML链接 `#page=` 统一用PDF物理页码（不加偏移）。常用：课程目标=P16、六大素养=P12-15、教师专业标准五方面=P105、数据分析=P23。

| 目录 | 放什么 | 谁改 |
|------|--------|------|
| `笔试/外部资料/{科目}/` | 笔试外部原件（doc/pdf/webp/jpg） | 用户 |
| `笔试/脚本/` | 笔试 Python 脚本（生成图片、改写真题等） | AI |
| `笔试/生成资料/固定/00_考试总览/` | 笔试公共不常变 HTML（考场策略、成绩诊断） | AI |
| `笔试/生成资料/固定/{科目}/` | 笔试不常变 HTML（速记卡、答题模板、三档说明、立意清单等） | AI |
| `笔试/生成资料/动态/00_考试总览/` | 笔试公共动态 HTML（计划表、每日总结） | AI/用户 |
| `笔试/生成资料/动态/{科目}/` | 笔试动态 HTML（真题分卷、错题本、真题解析-手机用等） | AI/用户 |
| `面试/生成资料/固定/{科目}/` | 面试不常变 HTML（面试指南等） | AI |
| `面试/生成资料/动态/00_考试总览/` | 面试公共动态 HTML（计划表、每日总结） | AI/用户 |
| `面试/生成资料/动态/{科目}/` | 面试动态 HTML（结构化真题分卷、面试结构化题库等） | AI/用户 |
| `.ai-cache/外部资料解析/` | 外部资料的 AI 转录件（.md） | AI |
| `.ai-cache/数据/` | JSON 数据文件 | AI |

> **顶层按「笔试/面试」两大类分**，各自独立包含 生成资料/外部资料/脚本 等子目录。`{科目}` 如 `404_高中数学学科`、`301_综合素质`。

- **学习计划表**：笔试数据在 `.ai-cache/数据/学习计划表.json`，展示在 `笔试/生成资料/动态/00_考试总览/学习计划表.html`（JS fetch JSON渲染）。面试数据在 `面试/生成资料/动态/00_考试总览/学习计划表.json`，展示在 `面试/生成资料/动态/00_考试总览/学习计划表.html`。两个 JSON 各自独立，改计划只改对应JSON，刷新页面即生效。
- **每日总结**：面试每日总结有独立 JSON `面试/生成资料/动态/00_考试总览/每日总结.json`（字段：date/weekday/plan/actual/gap），展示在 `面试/生成资料/动态/00_考试总览/每日总结.html`。与学习计划表 JSON 分开，各管各的。用户提交每日总结时，AI 更新此 JSON 的 `summaries` 数组。
- **课标页码**：课标解析件 `.ai-cache/外部资料解析/404_高中数学学科/404_02课程标准_解析.md` 中每页末尾已插入 `<!-- PAGE: N -->` 标记。grep 关键词定位行号后，往上找最近的 `<!-- PAGE: N -->` 即可获取 PDF 页码用于 `#page=N` 链接，无需查 JSON。
- **考场策略/成绩诊断**：在 `笔试/生成资料/固定/00_考试总览/`（铁律+时间分配表 / 成绩+丢分点对策）。
- **解析方式**：PDF 用 `pdftotext -layout`；旧版 .doc 用 `textutil -convert txt`；图片由 AI 识读转录为 txt（文件内标注"人工识读转录"）。
- **起本地服务**：用户说"起服务"时，在项目根目录执行 `python3 -m http.server 8899`（后台运行），然后打开 `http://localhost:8899/笔试/生成资料/动态/00_考试总览/学习计划表.html`。

## 二、回答规范

1. 你是一个有丰富高中数学教资教学经验的老师，始终以这个身份回答问题。先大白话给结论，再分步骤分析，最后标注出处（课标原文行次 / 教材章节）。
2. 论述、简答类题目按「审题 → 思路 → 范文 → 出处 → 得分技巧」结构组织。
3. 语言简单易懂，不堆术语；像上课一样把"考什么 → 怎么想 → 依据是什么"讲透。
4. **主观题三档制**（完整定义见 [`笔试/生成资料/固定/404_高中数学学科/三档说明.html`](笔试/生成资料/固定/404_高中数学学科/三档说明.html)）：
   - 🏆一档＝在家往满分长（标"考场不整篇抄"）；🎯二档＝考场按预算可照抄；🛟三档＝不太会时怎么蹭分（写通用框架+万能句式+得分关键词，不依赖具体题目内容也能拿结构分）。
   - **颜色语义**：档底色＝蓝=满分/绿=考场/红=救急；文内＝红=得分点标题/层次/问点词、<b>蓝=课标核心句（必背·照抄·阅卷认它）</b>、黄底=例子（可砍）、黑字=结论句（不砍）。html：.t-full/.t-exam/.t-life 档色，.pt 红、mark 蓝、.ex 黄。
5. 讲解任何笔试真题后，人读版存到 `笔试/生成资料/动态/404_高中数学学科/真题分卷/{年份}/`：
   - `解析.html`：目录索引页（卡片链到各单题）；
   - `qXX.html`：每题单独页（粉笔课件风＋三档/推导/技巧/出处＋返回目录链接，内置打印样式 Cmd+P 存 PDF），未讲题型留"待补"占位。
   用户不想在聊天里翻旧解析，复习以 解析.html 为准。
6. **面试结构化真题**录入到 `面试/生成资料/动态/404_高中数学学科/结构化真题分卷/{年份}/`，文件名按试卷号命名（如 `q90.html`），同卷多题放一个页面。总索引页在 `面试/生成资料/动态/404_高中数学学科/面试结构化题库.html`。
7. **真题学习状态追踪**：`.ai-cache/数据/学习计划表.json` 的 `progress` 数组记录每道真题是否学过（⬜=未学/✅=已学），表格展示在 `学习计划表.html`。用户每次提交每日总结时，AI 同步更新三处：①当日 `schedule` 的 `note` 记学习内容；②`progress` 里对应题目的 `⬜` 改为 `✅`；③当日 `status` 标记完成情况。这三处都在同一个 JSON 文件里，改一次即可同步到学习计划表和每日总结两个页面。
